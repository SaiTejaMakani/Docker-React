sudo : required
services:
  - docker

language: generic 

before_install:
  - docker build -t saitejamakani/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true saitejamakani/docker-react npm run test -- --coverage